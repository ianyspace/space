/**
 * JVM 领域的术语库。
 *
 * 每一条对应正文里一个 `<term>…</term>`：`def` 是必填的一句话解释，`more` 是可选的
 * 一行补充。两者都只当纯文本渲染（不走 markdown），所以别写反引号或星号。
 *
 * key 只要求「读起来是这个词」，大小写和空格由 `content/glossary/index.js` 归一化，
 * 所以这里写 `'Minor GC'`、正文写 `<term>minor gc</term>` 也能对上。
 *
 * 收录标准：**正文自己没解释过的词**。文章里有专节、有就地一句说明、或者在表格里
 * 给了做法和优缺点的，一律不进这张表 —— 否则读者点开看到的还是刚读过的内容。
 */
const jvm = {
    OOM: {
        def: 'OutOfMemoryError，内存真的不够了。',
        more: '注意和「内存泄漏」区分：泄漏是对象没用了却还被引用着、GC 收不掉，攒够了才会演变成 OOM。',
    },
    HotSpot: {
        def: '目前最主流的 JVM 实现，平时说的「JVM」基本就是指它。',
        more: 'Oracle JDK 和 OpenJDK 用的都是 HotSpot。',
    },
    'Minor GC': {
        def: '只回收新生代的 GC。',
        more: 'Eden 满了就触发，次数多、每次停顿短。',
    },
    'Major GC': {
        def: '通常指只回收老年代的 GC。',
        more: '各版本 JDK 对它的定义并不统一，有的文档里它和 Full GC 是同义词。',
    },
    'Full GC': {
        def: '回收整个堆 —— 新生代、老年代和方法区一起收。',
        more: '停顿最长，调优的一大目标就是减少它的触发次数。',
    },
    对象头: {
        def: '对象在堆里除实例字段之外的那段元信息。',
        more: '哈希值、GC 分代年龄、锁状态标志都存在这里，是 JVM 认识一个对象的地方。',
    },
    符号引用: {
        def: '用「类名 + 字段名 / 方法名」这种字符串形式描述的引用。',
        more: '还没落到内存地址上，解析阶段才会换成直接引用。',
    },
    运行时常量池: {
        def: '方法区里存放字面量和符号引用的那块区域。',
        more: '类加载时把 class 文件里的常量池搬进来，解析阶段在这里把符号引用换成直接引用。',
    },
    类卸载: {
        def: '把一个不再使用的类从方法区里清掉。',
        more: '条件很苛刻：该类的所有实例都已回收、加载它的 ClassLoader 已被回收、Class 对象也没有被引用。',
    },
    'Mark Word': {
        def: '对象头里记录运行时数据的那部分。',
        more: '哈希值、GC 分代年龄、锁状态标志都在里面；年龄字段只有 4 bit，所以最大值就是 15。',
    },
    SPI: {
        def: 'Service Provider Interface，服务提供者接口。',
        more: '接口和实现分离，靠 META-INF/services/ 下的配置文件找实现类，JDBC 驱动就是典型。',
    },
    classpath: {
        def: '启动时告诉 JVM 去哪儿找 .class 文件的路径列表。',
        more: '应用类加载器就是按它来加载我们自己写的类的。',
    },
    JNI: {
        def: 'Java Native Interface，Java 调用 C/C++ 代码的那层接口。',
        more: '本地方法栈里存的就是它的引用，native 方法执行时靠它回到 JVM。',
    },
    吞吐量: {
        def: '用户代码运行时间占总时间的比例。',
        more: '和停顿时间天生互相拉扯：想让它高，通常就得容忍更长的单次停顿。',
    },
    停顿时间: {
        def: '一次 GC 让程序停下来多久。',
        more: '和吞吐量是一对矛盾，调优就是在这两者之间选一个平衡点。',
    },
    直接内存: {
        def: '堆外的那部分内存。',
        more: 'NIO 的 DirectByteBuffer 用它，不受 -Xmx 限制，但受机器物理内存限制。',
    },
};

export default jvm;
