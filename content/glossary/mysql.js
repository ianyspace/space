/**
 * MySQL 领域的术语库。
 *
 * 每一条对应正文里一个 `<term>…</term>`：`def` 是必填的一句话解释，`more` 是可选的
 * 一行补充。两者都只当纯文本渲染（不走 markdown），所以别写反引号或星号。
 *
 * key 只要求「读起来是这个词」，大小写和空格由 `content/glossary/index.js` 归一化，
 * 所以这里写 `'随机 IO'`、正文写 `<term>随机IO</term>` 也能对上。
 *
 * 收录标准：**正文自己没解释过的词**。文章里有专节、有就地一句说明、或者在表格里
 * 给了做法和优缺点的，一律不进这张表 —— 否则读者点开看到的还是刚读过的内容。
 */
const mysql = {
    数据页: {
        def: 'InnoDB 存取数据的基本单位，默认 16 KB 的一块连续空间。',
        more: '不管一行数据多小，磁盘 I/O 都按整页来读写；算树高、缓存、脏页刷盘都是围绕「页」进行的。',
    },
    '随机 IO': {
        def: '读写位置在磁盘上跳来跳去，机械硬盘每次都要重新寻道，比顺序读写慢几个数量级。',
        more: '回表就是随机 IO；WAL 之所以快，就是把随机写数据页换成了顺序写日志。',
    },
    页分裂: {
        def: '页写满了还要往它中间插一条数据，InnoDB 只能把这页一分为二，两边各留一半，父节点跟着改。',
        more: '自增主键永远追加到最后一页，基本不触发；UUID 这类乱序主键会频繁触发，写入慢还浪费空间。',
    },
    区分度: {
        def: '一列里不同值的数量占总行数的比例，越高越能一步筛掉无关的行。',
        more: '性别这类只有两三个值的列区分度极低，单独建索引大多数查询都用不上。',
    },
    DDL: {
        def: 'Data Definition Language，数据定义语言，改表结构的那类语句：CREATE、ALTER、DROP。',
        more: '只碰表定义不碰行数据，但 MDL 的存在让它随时可能把整张表的读写堵死。',
    },
    DML: {
        def: 'Data Manipulation Language，数据操作语言，改数据的那类语句：INSERT、UPDATE、DELETE。',
        more: 'binlog 记录的就是 DDL 和 DML，SELECT 不记。',
    },
    脏页: {
        def: '在内存里被改过、还没写回磁盘的数据页。',
        more: '它和磁盘上的旧数据暂时不一致；有 redo log 兜底，所以可以放心等后台线程慢慢刷盘。',
    },
    主从复制: {
        def: '主库把 binlog 发给从库重放，让从库持有同一份数据，读请求可以分流到从库。',
        more: 'binlog 是主从之间同步的唯一凭证，两阶段提交拿它做仲裁也源于此。',
    },
    checkpoint: {
        def: 'redo log 上的一条分界线：这之前的改动都已经刷到磁盘，对应的日志空间可以覆盖重用。',
        more: 'write pos 是当前写入位置，两者之间是还没刷盘的部分；write pos 追上 checkpoint 就得停下来等刷盘。',
    },
    fsync: {
        def: '操作系统调用，强制把数据从缓存真正写到磁盘上。',
        more: '写到 OS page cache 还不算落盘，断电会丢；fsync 之后才算真正持久化，这就是刷盘参数取 1 最安全也最慢的原因。',
    },
    驱动表: {
        def: 'JOIN 时放在外层循环的表：先查出它的行，再逐行拿去被驱动表里匹配。',
        more: '驱动表只查一次、被驱动表被查 N 次，所以让小表当驱动、给被驱动表的连接条件建索引。',
    },
};

export default mysql;
