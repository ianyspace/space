/**
 * Java 领域的术语库。
 *
 * 每一条对应正文里一个 `<term>…</term>`：`def` 是必填的一句话解释，`more` 是可选的
 * 一行补充。两者都只当纯文本渲染（不走 markdown），所以别写反引号或星号。
 *
 * key 只要求「读起来是这个词」，大小写和空格由 `content/glossary/index.js` 归一化，
 * 所以这里写 `'OOM'`、正文写 `<term>oom</term>` 也能对上。
 *
 * 收录标准：**正文自己没解释过的词**。文章里有专节、有就地一句说明、或者在表格里
 * 给了做法和优缺点的，一律不进这张表 —— 否则读者点开看到的还是刚读过的内容。
 */
const java = {
    上下文切换: {
        def: 'CPU 从一个线程切到另一个线程执行的过程：保存当前现场、恢复下一个现场。',
        more: '切换本身不干活还费时间，线程开得太多时大量时间耗在切换上——这是线程数不是越多越好的根因。',
    },
    内核态切换: {
        def: '线程挂起/唤醒要陷入操作系统内核、由内核完成调度，这一来一回的切换。',
        more: '比纯用户态操作慢一个量级，所以自旋、CAS 这些手段的价值就是能免则免。',
    },
    缺页中断: {
        def: '线程访问的数据不在物理内存里，要等操作系统从磁盘把页调进来，期间线程被暂停。',
        more: 'CPU 密集型线程也会因此偶尔暂停，这正是线程数公式里 +1 的由来。',
    },
    装饰者模式: {
        def: '不改动原类、靠一层层包装动态叠加功能的设计模式。',
        more: 'IO 流里每包一层多一种能力：FileInputStream → InputStreamReader → BufferedReader 就是典型。',
    },
    开闭原则: {
        def: '对扩展开放、对修改关闭——加新功能靠新增代码，不去改已有代码。',
        more: '工厂方法新增产品只需加一个工厂类、不动旧代码，所以「符合」它。',
    },
    ASM: {
        def: '一个直接读写、生成字节码的 Java 框架。',
        more: 'CGLIB 在运行时生成代理类，底层靠的就是它来写字节码。',
    },
    epoll: {
        def: 'Linux 提供的 IO 多路复用机制，让一个线程同时监听大量连接上的事件。',
        more: 'NIO 的 Selector 在 Linux 上底层就是它，连接多、活跃少时远比 select 高效。',
    },
};

export default java;
