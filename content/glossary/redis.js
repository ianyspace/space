/**
 * Redis 领域的术语库。
 *
 * 每一条对应正文里一个 `<term>…</term>`：`def` 是必填的一句话解释，`more` 是可选的
 * 一行补充。两者都只当纯文本渲染（不走 markdown），所以别写反引号或星号。
 *
 * key 只要求「读起来是这个词」，大小写和空格由 `content/glossary/index.js` 归一化，
 * 所以这里写 `'IO 多路复用'`、正文写 `<term k="IO 多路复用">多路复用</term>` 也能对上。
 *
 * 收录标准：**正文自己没解释过的词**。文章里有专节、有就地一句说明、或者在表格里
 * 给了做法和优缺点的，一律不进这张表 —— 否则读者点开看到的还是刚读过的内容。
 */
const redis = {
    'IO 多路复用': {
        def: '一种 IO 模型：一个线程同时监听多个连接，哪个连接有数据就处理哪个，没数据时不干等。',
        more: 'Redis 单线程却能扛高并发就靠它；Linux 下的实现是 epoll，Java NIO 的 Selector 底层也是它。',
    },
    Redisson: {
        def: 'Java 里最常用的 Redis 客户端，把分布式锁、布隆过滤器这些能力封装成现成的 API。',
        more: '它的分布式锁自带看门狗自动续期，不用自己操心锁过期。',
    },
    Raft: {
        def: '一种分布式选主协议：节点之间投票，拿到多数票的成为 Leader。',
        more: 'Redis 哨兵选 Leader 用的是它的简化版，etcd、Consul 的选主也基于它。',
    },
    etcd: {
        def: '一个强一致的分布式键值存储，常用来做配置中心和服务发现。',
        more: 'Kubernetes 用它存集群状态；做分布式锁时它比 Redis 更强调一致性（CP）。',
    },
    Zookeeper: {
        def: '一个分布式协调服务，提供临时节点、顺序节点和监听机制，常用来做分布式锁和注册中心。',
        more: '和 Redis 锁的区别是它走 CP：靠会话和临时节点判断持有者是否还活着，一致性更强。',
    },
    'AP 与 CP': {
        def: 'CAP 定理里的两种取舍：AP 保证可用但可能读到旧数据，CP 保证一致但可能拒绝服务。',
        more: 'Redis 分布式锁属于 AP——追求能用，极端情况下锁可能失效；Zookeeper / etcd 属于 CP。',
    },
};

export default redis;
